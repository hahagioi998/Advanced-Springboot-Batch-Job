# Advanced-Springboot-Batch-Job
This project shows how to read a csv file with data, transform the data or manipulate it, then write it to another file or db.

# How to run
Checkout the project and run it

The csv content will be read, processed and saved into H2 database
